# ✂️ Code Snippets

Store reusable blocks of code, complex TypeScript types, or useful Tailwind UI patterns here.

Example snippet:
```tsx
export function useDebounce<T>(value: T, delay: number = 500) {
  // ... implementation
}
```
